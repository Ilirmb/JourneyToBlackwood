using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Under Construction by Steve L.
//Will control player animation to set wobble on animation during log platforming.

public class WobbleBabyWobbleBabyWobbleBabyWobble : MonoBehaviour
{
    GameObject Player;

    // Start is called before the first frame update
    void Start()
    {
        Player = GameObject.FindWithTag("Player");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
